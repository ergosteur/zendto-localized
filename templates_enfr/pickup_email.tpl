Ceci est un message automatisé de la part du service {#ServiceTitle#}. 

Le partage que vous avez crée (ID: {$claimID}) a été téléchargé.

{$whoWasIt} a accédé au partage depuis {if $hostname eq $remoteAddr}{$remoteAddr}{else}{$hostname} ({$remoteAddr}){/if}.

--------

This is an automated message sent to you by the {#ServiceTitle#} service. 

The drop-off you made (claim ID: {$claimID}) has been picked-up.

{$whoWasIt} made the pick-up from {if $hostname eq $remoteAddr}{$remoteAddr}{else}{$hostname} ({$remoteAddr}){/if}.

